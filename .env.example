# Notion internal integration secret (starts with "ntn_" or "secret_")
# Create one at https://www.notion.so/my-integrations
NOTION_TOKEN=

# The database ID (from the database URL) that has been shared with the integration above
NOTION_DATABASE_ID=
